require "spec_helper"

describe WaterCannon do
  it "has a version number" do
    expect(WaterCannon::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
